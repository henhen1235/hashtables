#include <iostream>
#include <cstring>
#include "node.h"
#include "student.h"
#include "hashtables.h"
#include <vector>


using namespace std;

hashtables::hash(int size){
  vector<student*> table(size);
}

int hashtables::hashkey(int key){
}

void hashtables::insert(int key, student*){
  int nkey = hashkey(int key);
  
}
